
import { LucideIcon } from "lucide-react";

export interface CategoryStyle {
  emoji: string;
  colors: {
    bg: string;
    border: string;
    hover: string;
    selected: string;
  };
  icon: LucideIcon;
}
